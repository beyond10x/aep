# ESS complete-selection evidence — AEP reader wave

**Skill version 0.8.0 — aep-drive:wave.** This is one separately governed prerequisite for
ESS story:review-conformance-coverage, serving vision:O2. The operator's standing ESS remediation
implementation and green-publication approvals cover this dependency, its review and task-owned
managed cleanup. They authorize opening and closing record commits, unit and correction commits,
integration merges, green main publication and required documentation delivery. No release, tag,
version bump, installation or default switch is selected.

## Selection

Select only story:admit-ess-conformance-coverage. Its count-reader dependency is implemented and
published. The five existing source packages jointly own the new carrier, exact-byte admission,
independent qualification, mutation and replay routes. Splitting them into independent units would
leave a required admission boundary incomplete. N is one; the later ESS writer is a separate wave.

The fresh global draft computation is retained verbatim in the
[dated review record](../reviews/2026-09-06-ess-conformance-coverage-replan.md).
It contains fourteen candidate sets, 282 collisions and twelve unassessed unrelated AEP stories.
Those stories are outside the user's ESS remediation task; this coordinator neither selects nor
reserves their work. As in the count-reader prerequisite, the bounded selection uses the complete
proposed set after the CLI moves only this prerequisite to proposed. That computation has one unit,
no collisions, no unassessed artifact and no cycle. This scope restriction is explicit; the global
unassessed set is not being called safe or silently omitted.

The independent story-scoper established five cited production reservations. The new binding,
opt-in policy leaves, generated schema leaves, public CLI reference and changelog remain inferred
until implementation confirms them. The root owns the planning journal and shared design records.
No other AEP implementation is dispatched by this wave.

## Binding and review

The accepted docs/design/ess-conformance-coverage-evidence.md owns the separate
ess_conformance_coverage_v1 carrier, independent task expectation and shared per-record decision.
ESS owns the accepted coverage contract and input/1 transport supplement. Atlas ADR0040
coordinates the known relying parties and reader-first shipment. Both binding reviews are recorded
through AEP in both repositories. The first found the incomplete observable table and stale strict
story wording; the second confirms those corrections and found one Text truthiness sentence. Root
checked its final wording against the unchanged FactValue/predicate owner. The final contracts
are accepted through the owning stories under the standing remediation grant. No test result is
inferred from document review.

Review snapshots, exact hashes and source heads are retained in the ESS coordinator's
target/review-boundaries-9/binding-review-1. This is a review of proposed documents, separate from
the closed count-writer unit and from the new reader's later implementation attacks. It establishes
no executed compatibility test or implementation verdict.

## Resource and ownership record

The reused AEP coordinator is
/home/timo/.local/state/worktree/trees/b10x/aep/ess-conformance-v2-reader,
branch wave/ess-conformance-counts, lease ess-review-counts-reader, source
62ef3a73112143453319215b9aa31a6c9626ed5f. That source matched advertised AEP main at preflight.
Only this task's new story, journal events and proposed binding were dirty. The primary is dirty
at e27c84b and remains untouched; the full worktree inventory is retained in preparation scratch.
Other sessions' linked trees and branches are preserved.

Reserve managed implementation ID aep-ess-conformance-coverage and branch
impl/ess-conformance-coverage. The manager-returned worktree path, exact opening base and lease
will be recorded before dispatch. Its build directory is its own target; its scratch is its own
target/ess-conformance-coverage. No unit tree exists yet. The old count implementation and Website
verification trees are removed, with archived evidence verified. The reused coordinator targets
are deliberate resources: AEP 3,472,422,957 bytes, ESS 7,262,422,040 bytes and Atlas 1,021,631,965 bytes
at the recorded measurement. They are not abandoned unit build directories.

Preflight measured 22,217,244,672 free bytes against an 8,589,934,592-byte floor and 49,319,688 KiB
available memory. One implementation slot fits within three available workers. No numerical model
budget was supplied; the existing default of at most four total agents remains, with no model
override. Agent token/tool/wall-cost metrics are unavailable from the harness.

Use direct Rust compilation: unset CARGO_TARGET_DIR, RUSTC_WRAPPER and SCCACHE_SERVER_UDS;
disable incremental and debug information, use four build jobs and offline Cargo. sccache 0.16.0
exists but the recovered run explicitly uses direct compilation after shared-cache failures.
No shared compiler daemon or target is introduced. Rust is 1.98.0, Cargo is 1.98.0, Task is 3.52.0.
The required MSRV lane still uses the actual installed 1.85 toolchain. Node 24 is used for website
checks. PostgreSQL is explicitly skipped when its configured URL is absent.

The measured existing-source package baseline and its exact logs live in
target/ess-conformance-coverage/preparation. Its first attempt stopped after two doctor alias
comparisons returned different exit codes while other work was reading the store; the earlier
test summaries remain a partial baseline. The cause is not yet established. No assertion was
changed or result suppressed. Two direct doctor calls and the unchanged focused alias target then
passed. The complete same-command quiet baseline passed 1,320 cases, zero failed or ignored, across
61 runner summaries in 133.120 seconds. Peak child RSS was 261,712 KiB, the warm coordinator target
was 3,546,398,515 bytes and free disk was 21,548,732,416 bytes. This is a warm source measurement,
not an invented cold-build estimate. All production bytes were unchanged at62ef3a7.
The initial orchestration could not spawn absent /usr/bin/time; Cargo had not run in that setup
attempt. The corrected measurement uses an observed monotonic interval and child resource usage.

## Implementation and validation

Roles are aep-drive:implementor and aep-drive:adversary. The collaboration harness has no native
plugin role selector; its threads read the exact installed 0.8.0 charters, the established adapter
deviation. Root writes the unit brief to its assigned scratch before dispatch and owns all Git,
planning, shared binding/changelog, integration, publication and cleanup operations.

The implementor owns the five reserved packages, three additive policy leaves, public CLI
reference and actual generator-produced schema changes. It confirms inferred paths before editing,
retains existing source controls, writes and executes behavioral failures before fixes, and proves
new guards through mutations. Missing-API compilation errors are not behavioral failures. The
independent expected suite and selection must not be copied from a report under test.

Run package tests, package formatting and strict all-target package Clippy, retaining each actual
command, exit and executed-case count. The adversary receives the frozen source and all previous
controls; at most two full reader implementation attacks are authorized by the normal wave rules.
Every review and disposition is recorded through AEP. Only a green unit integrates.

Root gates the opening metadata before creating the unit. After integration it runs all sixteen
declared AEP gate lanes, captures their own exits and reads every skip. Publication includes the
public CLI source and required immutable Website/Atlas verification. Publish the exact reviewed
AEP reader before the ESS suite/5 writer; actual frozen Rust/Go report/input pairs must then pass
both AEP readers and typed replay before ESS source publication. Source shipment does not imply
installed-reader or retained generated-runtime adoption.

## Current stage

Bindings accepted at AEP4ecc1b63, ESS e339f4ce and published Atlas21dd01f1. Both author and committer
on all direct commits were verified as the organization bot. AEP's ten opening checks passed;
ESS's layout target and required site-build passed. These checks cover the opening records and
unchanged existing source; no new reader implementation has been tested.

The manager created /home/timo/.local/state/worktree/trees/b10x/aep/aep-ess-conformance-coverage
at exact base4ecc1b63b53bbfcb31fd55f277e62643083375b1. Its branch is impl/ess-conformance-coverage,
lease ess-review-coverage-implementation, build directory its target and scratch its
target/ess-conformance-coverage. The root-written unit-brief.md and hash-verified accepted bindings
plus source-baseline logs are there before dispatch. Test TMPDIR alone uses the explicitly assigned
/home/timo/.cache/ess-review/2026-09-06-resume/aep-coverage-unit-tmp because the existing
outside-project fixture requires a directory outside a project. That exception is unit-owned,
reported and part of cleanup; no compiler output moves outside the unit target.

Implementation is frozen at48efe3783b559caf17f7d70510935e253d8b3d6f with both bot identities
verified. The first source adversary is running under aep-drive:adversary0.8.0 in the same unit,
with tests-only ownership in the five assigned packages. Its scratch is
target/ess-conformance-coverage/adversary-pass-1, build is the unit target, and its distinct
explicit outside-project TMPDIR is
/home/timo/.cache/ess-review/2026-09-06-resume/aep-coverage-adversary-1-tmp.
The implementor relinquished all writes before freezing and review dispatch.

The final five-package run executed1360 cases,0failed/ignored,65 summaries;40 cases are new.
The initial full run retained1357passes and3failures. One failure required additive protocol/profile
inventory expectations; two unchanged instruction tests exposed the existing renderer's global
principle enumeration. Root applied the actual generator's six changed instruction leaves,
with explicit opt-in wording; README stayed byte-exact. Existing workflows, renderer and default
profiles were not changed. The public reference states the existing rendering limitation.
All unchanged count, report and default-policy controls execute in the final green run.

Strict package all-target Clippy, formatting, actual schema generation and drift checks each
exited0. The generator changed exactly10 expected schema leaves. Ten named guard mutations each
compiled and failed its selected assertion, then restored byte-exact; all original failures,
setup failures, commands and restoration hashes are retained. No dependency or lockfile changed.
Root verified all239 final manifest entries and47 changed/new paths before the bot commit.
Implementor report SHA256266914db656135b55418d436dd8e1bbf5403f569cc738473ccfd2a62385d3717;
manifest SHA256357724cae32d749fb961e56e3ba1b3b4291efcc7b68308a2da60693561105647.

The separate scratch correspondence harness has only setup-compiled. Its22 preparation records
were hash-verified, and the retained actual count-stage Rust control was copied byte-exact from
the closed count-writer evidence. No helper runtime, actual suite/5 producer output or producer
correspondence exists yet. Full repository integration gates and publication remain pending.
The complete computed outputs are in the companion dated review record.

## First source attack and correction

The immutable first source attack is recorded as
review-result:ess-conformance-coverage-source-adversary-pass-1, report SHA256
d83f6aa2528da72ceffa71abcded5945ed17a3bdbf8ca7797665d51ea6fa2fad. Root verified its full
execution-file catalog and manifest SHA256
f50d7f00d4ce6f79c639f63154ec82a28d084cd6e14a6caa06e0fcb58c88590f. Two additive test paths,
262 new lines and no removed assertions were frozen at29b4d30628389429729514bd97cae62accf732fb
with both bot identities verified. The complete five-package attack executed1366 cases:
1363passed,3failed,0ignored,66 summaries. All1360 inherited cases and3 new controls passed.
Final formatting and strict all-target Clippy passed; the original helper compile failure and
test-only semicolon correction remain separately recorded.

Two introduced findings cover raw-presence comparison of inherited optional defaults and
exact-u64 comparison at known finite Node payload positions. The report preserves the latter
interpretation separately: root follows the accepted ESS binding's actual Node domain only at
typed Node positions; exact u64 fields, all original byte hashes and other survivor metadata
remain exact. This permits no global rounding or discarded null payload keys. Story revision28
records that narrow decision and the correction handoff. The same implementor is correcting the
complete class in its existing unit under target/ess-conformance-coverage/correction-pass-1,
using its original assigned external TMPDIR. All previous source and adversary assertions remain.
The second and final source attack, repository gates and publication are still pending.
